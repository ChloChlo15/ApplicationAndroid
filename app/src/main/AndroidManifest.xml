<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lac_temperature_tp5">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SaisirTemperature"></activity>
        <activity android:name=".SaisirTemperatureMois"></activity>
        <activity android:name=".SaisirTempJours"></activity>
        <activity android:name=".BddTempJour"></activity>
        <activity android:name=".MoyenneTemp"></activity>
        <activity android:name=".AffichageTempJour"></activity>

    </application>

</manifest>